obj-m += xxx.o

export ARCH=arm64
export CROSS_COMPILE=aarch64-mix210-linux-
KDIR := ../../../../linux-4.19.y
PWD := $(shell pwd)
all:
	$(MAKE) -C $(KDIR) M=$(PWD) modules
clean:
	$(MAKE) -C $(KDIR) M=$(PWD) clean